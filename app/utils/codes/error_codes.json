{
  "PASSWD_FILE_NOT_FOUND": {
    "description": "passwd file missing.",
    "fix": "The /etc/passwd file was not found in the specified path. Check if the file is present and the service has the required permissions."
  },
  "PASSWD_FILE_PERMISSION_DENIED": {
    "description": "Permission denied to access /etc/passwd.",
    "fix": "Ensure the service has the appropriate permissions to read the /etc/passwd file."
  },
  "NO_USERS_FOUND": {
    "description": "No users found matching the specified criteria.",
    "fix": "Ensure there are users that match the provided status or criteria."
  },
  "UNEXPECTED_ERROR": {
    "description": "This error is not listed or not known.",
    "fix": "Report this error to the dev team with relevant details, including what actions led to the error, so we can investigate."
  },
  "CHAGE_COMMAND_FAILED": {
    "description": "Failed to retrieve user password details using the chage command.",
    "fix": "Ensure the chage command is installed and accessible with sufficient permissions."
  },
  "CHAGE_COMMAND_NOT_FOUND": {
    "description": "The chage command was not found on the system.",
    "fix": "Install the chage utility or verify your PATH environment variable."
  },
  "CHAGE_PERMISSION_DENIED": {
    "description": "Permission denied while attempting to run the chage command.",
    "fix": "Run the command with elevated privileges or adjust user permissions."
  }
}
